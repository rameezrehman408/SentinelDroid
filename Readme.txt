Currently this project is:

Taking 1029 size int input (formed in the shape of input_val named object) and provides a 1 float point output.

Loops through an installed app's permission part of the manifest and update the input_val named object accordingly.

FrontEnd is finalized.
______________________________________________________________________________________________________________
Further improvements required:

Loop through intent section of the manifest file as well and update the input_val named object accordingly.